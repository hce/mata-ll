[package]
name = "mll"
version = "0.1.0"
edition = "2024"

[dependencies]
mllc = { path = "../mllc" }
mlua = { version = "0.12", features = ["lua54", "vendored"] }
clap = { version = "4.6", features = ["derive"] }
